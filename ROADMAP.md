# DuckPipe — Project Roadmap & Design Spec

*A serverless-first, DuckDB-native pipeline orchestrator. Working title: **DuckPipe** (alternatives: QuackFlow, DuckOrc, FlockWork — see Naming below). Status: Phases 0-2.5 (§11) implemented and validated, plus all of Phase 3 (3a task-scoped distributed execution, 3b DuckLake observability upgrade, 3c serverless executor, 3d beefy-node mode) and Phase 4's first real deliverable (local-only browser execution via Pyodide) — see `README.md` for the runnable library this doc now describes retroactively as much as prospectively. Remote sync in-browser (`fsspec`-in-Pyodide) remains the next Phase 4 spike, not started.*

---

## 0. Naming note

"DuckFlow" is taken — there's an existing PyPI package (`duckflow`, github.com/markconde/duckflow) in an adjacent pipeline-framework niche, plus unrelated commercial brands using the name elsewhere. Checked and currently available on PyPI/GitHub with no conflicting DuckDB-orchestration project: **duckpipe**, **quackflow**, **duckorc**, **flockwork**, **featherflow**, **soloduck**. This doc uses **DuckPipe** as the working name; treat it as a placeholder until you lock in a final choice and do a proper trademark screen.

---

## 1. Vision

Most teams reach for Airflow, Prefect, or Dagster the moment they need "more than one script that depends on another script." All three solve that by asking you to first stand up persistent infrastructure — a scheduler, a webserver, a metadata database, and usually a broker/worker pool — before you've run a single real pipeline. That infrastructure tax is fixed cost, paid whether your pipeline moves a few thousand rows once a day or a billion rows every minute.

DuckDB's own rise made a similar point about query engines: a huge share of "big data" workloads people defaulted to Spark for were never actually big-data workloads — they fit comfortably on one modern machine, and a single well-tuned embedded process finishes in seconds what a cluster took minutes to schedule. DuckPipe applies that same correction to orchestration: **most pipeline DAGs are a handful of dependent steps operating on data that fits on one node.** The orchestrator for that case should be a library, not a platform — importable, runnable as a plain Python process, requiring zero standing infrastructure, and disappearing entirely when it's not running.

**The bet:** if we get the single-node, zero-server experience right — genuinely as easy as `pip install duckpipe` and running a Python file — there's "almost no reason not to use it," the same way there's rarely a reason to reach for Spark before trying DuckDB first. Distributed/remote execution is a deliberate Phase 3+ extension of the same core, not a redesign.

---

## 2. Design tenets (the non-negotiables)

1. **No persistent servers required to get started.** No scheduler daemon, no central metadata database, no broker. A DuckPipe run is a process that starts, does work, records what it did, and exits. State lives in a file (a DuckDB database) by default, not a running service. This is a default, not a restriction: an opt-in DuckLake catalog upgrade (§8, Phase 3b) is fully compatible with a centralized-metadata-store architecture when a team wants one — including one backed by a dedicated, long-running database (Postgres, self-hosted or managed) shared across several DuckPipe deployments for genuine multi-user/multi-tenant use — it's just never the thing you have to stand up before running your first pipeline. In v1, the plain-file state can optionally be synced to durable remote storage (S3/GCS/Azure/etc.) via `fsspec` — download-before-run, upload-after-run — so DuckPipe stays correct even when the process itself runs in an ephemeral, container-per-invocation environment (its own laptop/CI runner, or nested inside another orchestrator's worker — see §9). This is a sync pattern, not a live remote database: DuckDB's own docs confirm its native database file format only supports read-only remote attach (`ATTACH 's3://...'` via `httpfs`), not read-write, so a local-scratch-copy-then-sync approach is the correct one, not a workaround.

2. **The orchestrator core is data-blind. DuckDB is the orchestrator's brain (its state store), never a requirement for what a task does.** Keep two claims separate. First, the *orchestrator's own state* (run history, task fingerprints, logs, lineage) always lives in a DuckDB file/catalog — that's the core architectural bet (§4) and isn't optional. Second, and this is the tighter, more important rule than earlier drafts of this doc had: **a task's signature is `Callable[..., Any]`, and the core never inspects, converts, or reasons about what a task returns.** In v1 (single process, single node), passing a task's output to the next task is a plain Python function call — whatever the object is (a DuckDB relation, a Polars `LazyFrame`, a pandas DataFrame, a string, `None`) crosses zero boundaries and costs the orchestrator nothing, because there's no serialization to do. This is what makes "efficiency is a secondary bonus, not a core feature" literally true: DuckPipe adds no computational overhead on top of your data processing, because it never touches your data. Where a real boundary *does* exist — caching a result to disk between runs, or shipping a result to another process in Phase 3 — that's handled by an explicitly optional, separately documented layer (§6), never by the core.

3. **Never introduce a parallelism or tuning primitive that duplicates one an engine or Python already has.** Direct response to two related pain points we researched: Prefect ships `ConcurrentTaskRunner`/`DaskTaskRunner`/`RayTaskRunner` and steers users toward wrapping pandas in Dask inside tasks, adding a second parallelism model on top of a data engine that already parallelizes internally; and no orchestrator we found offers a *unified* resource-tuning abstraction across engines, because DuckDB's knobs (`threads`, `memory_limit`), Polars' (thread env vars, `collect(engine=...)`), and Daft's (runtime config) are genuinely different APIs — inventing one abstraction over all three would be exactly this kind of duplication, just relocated. DuckPipe's rule: **DAG-level concurrency** (running independent tasks concurrently) is the orchestrator's job, using a plain `asyncio`/thread pool invisibly. **Everything about a task's internal parallelism or resource tuning** — DuckDB's `threads`/`memory_limit`, Polars' thread count, a `concurrent.futures` pool, Ray/Dask for a genuinely distributed step — is the user's own code, using whichever engine's own native controls, optionally aided by small standalone utility functions (§6) the user calls themselves. The core has no opinion and no wrapper for any of it.

4. **Zero-to-running in one file, one command.** `duckpipe run pipeline.py` should work with no config file, no `duckpipe init`, no database migration step. A `duckpipe.db` file appears next to your script holding run history; that's the entire "infrastructure."

5. **Local dev and production are the same code path.** No "local executor vs. Celery executor vs. Kubernetes executor" branching that changes behavior. Scaling out (Phase 3+) changes *where* a task runs, never *how you write* a task.

6. **Incremental by default, not by manual flag.** Borrowing from SQLMesh: every task gets a content fingerprint (hash of its code + config + resolved upstream fingerprints — never the output data itself, which is what lets fingerprinting stay data-blind per tenet #2). If nothing changed, DuckPipe skips it. No `--select state:modified+` incantation required — this is on by default, with an explicit `--force` escape hatch.

7. **Small, boring surface area.** The full list of concepts a new user needs to learn should fit on one page. See §5. A DAG is inferred from plain Python function signatures, not a parallel YAML/config DSL — see §4 for why this is a deliberate, defended choice rather than a shortcut.

---

## 3. What we're explicitly *not* solving in v1

Distributed multi-node execution, a hosted UI/control plane, alerting/SLA infra, and dynamic-mapping-over-huge-fan-out (thousands of parallel tasks) are all out of scope for the initial phases. These are the things that make Airflow/Prefect/Dagster architecturally heavy, and they're exactly the cases where a single DuckDB process legitimately isn't the right tool. DuckPipe's stance: be *excellent* at the single-node case (which is most cases), and extend outward later via the DuckLake/Quack path (§8) rather than trying to be a general distributed-systems platform from day one.

Also explicitly out of the *core* (though available as opt-in companion utilities, §6): any engine-specific resource tuning (DuckDB PRAGMAs, Polars thread config, Daft runtime settings), and any built-in "efficient data handoff" machinery beyond a plain function call. Both were drafted as core subsystems in earlier iterations of this doc and were deliberately demoted — see §6 for why baking them into the core would have reintroduced the exact multi-engine complexity DuckPipe exists to avoid.

---

## 4. Landscape check — what already exists, what's a real gap

We looked for prior art before committing to this scope:

- **yato** (github.com/Bl3f/yato) — closest existing thing. A minimal SQL-only orchestrator: point it at a folder of `.sql` files, it infers dependencies and runs them via DuckDB. No Python task nodes, no scheduling, single-author/early-stage. Validates the appetite for this category; doesn't compete with the scope described here (fully generic Python tasks, DuckDB-backed state/fingerprinting, later distributed execution).
- **duckle** — a full no-code ETL/ELT platform on DuckDB (connectors, CDC, lineage). Different product category (Airbyte-alternative, not Airflow-alternative).
- **pydiverse.pipedag** — a caching layer that runs *on top of* Prefect. Doesn't remove the infrastructure dependency we're trying to remove.
- No project found uses **a DuckDB file itself as the DAG's metadata/coordination store**, and no project found uses **DuckDB/DuckLake on object storage as the coordination point for stateless serverless task execution** (the common pattern instead is AWS Step Functions + Lambda, where a managed service — not DuckDB — holds DAG state). Both are genuine, currently-unclaimed territory.

This means the core wedge — DuckDB file/catalog *as* the orchestrator's brain, coordinating tasks that are free to use whatever data-processing engine fits the job, or none at all — is real white space, not a reinvention.

**Why signature-based DAG inference over explicit YAML/config, resolved.** This is a legitimate question (Dagster/Prefect both use Python-native inference successfully, but it's fair to ask how it scales) and worth answering explicitly rather than leaving open. The usual scaling objection to implicit Python DAGs is actually an objection to one specific thing: a *persistent scheduler* that has to keep re-parsing/re-importing pipeline code to know the graph — this was Airflow's own documented bottleneck (§9, first row). DuckPipe has no persistent scheduler; `duckpipe run` imports the module exactly once per invocation, so that cost doesn't scale with DAG size or exist between runs at all. The other legitimate case for explicit config — non-Python-fluent stakeholders authoring pipelines, or needing to inspect DAG topology without executing code — doesn't match DuckPipe's target user (Python-fluent data/ML engineers writing their own tasks), and introducing a YAML DSL to serve it would recreate the exact "second language for control flow" complaint that pushes Airflow users toward Jinja-templated YAML in the first place. For large fan-out (hundreds of per-partition tasks), the answer is a plain Python loop generating uniquely-identified task instances — the same mechanism Dagster's dynamic outputs and Prefect's `.map()` already use — documented early as a first-class pattern rather than left as a gap.

---

## 5. The whole mental model (v1 concept surface)

Deliberately short. If this grows past this list without a very good reason, that's a design smell.

- **`@task`** — decorates a Python function. That's the entire contract: any Python function, any signature, any return type, and the core never looks inside what it returns. A task that happens to move data typically reads and returns whatever object its chosen engine naturally produces (a DuckDB relation, a Polars `LazyFrame`, a pandas DataFrame — DuckPipe doesn't care); a task that doesn't touch data at all (send a Slack alert, kick off a training run, write a log line) is equally first-class. No `@flow` wrapper concept separate from the DAG; the DAG *is* the set of tasks plus their declared dependencies.
- **Dependencies are inferred from function signatures**, not a separate graph-building API: if `task_b(x=task_a)` (or `task_b` type-hints an upstream task's output), DuckPipe knows the edge. An explicit `depends_on=[...]` escape hatch exists for side-effect-only dependencies. See §4 for why this scales fine without a YAML alternative.
- **A pipeline is a Python module.** `duckpipe run mymodule.py` discovers tasks in it and executes the DAG. No separate "deployment" object, no YAML.
- **State/history is a `.duckdb` file** next to the pipeline (path configurable). Every run, every task's status/duration/fingerprint/error is a row you can `SELECT * FROM duckpipe.task_runs` on directly — no separate UI required to know what happened, though a UI can be layered on top later by just querying this file.
- **Triggers are just "run the command."** Cron, a CI job, a webhook handler, a file-watcher, an AWS Lambda handler — all just call `duckpipe.run(...)`. DuckPipe does not ship its own scheduler daemon; use `cron`, GitHub Actions, or a cloud scheduler to decide *when*, and DuckPipe decides *what happens once triggered*. This sidesteps Airflow's scheduler-heartbeat/DAG-reparse bottleneck entirely by not having a long-lived scheduler process at all.
- **Retries/caching are per-task decorator args** (`@task(retries=3, cache=True)`), not separate "policy" objects to compose. `cache=True` fingerprints code+config+upstream-fingerprints only (tenet #6) — it never inspects the task's return value, so it works identically whether the task returns a DuckDB relation or `None`, and a side-effect task gets the exact same "only re-fire on a real change" behavior a data task gets, with no special-casing anywhere in the scheduler. The one caveat is generic to any retry-based system, not specific to this design: `retries>0` on a non-idempotent side effect risks repeating part of it on a retried attempt, same as it would in any orchestrator.

No work pools, no workers, no blocks, no deployments-as-a-separate-entity, no task-runner selection menu, and — per tenet #2 — no resource-profile object either; that's moved to the fully optional layer below. If you need to change *where* code runs, that's a Phase 3 executor setting, not a new concept.

---

## 6. Optional companion utilities (opt-in, outside the core)

Earlier drafts of this document put resource auto-tuning and a tiered lazy/eager/Arrow I/O model directly into the orchestrator core. On reflection that was a mistake worth naming explicitly: both required the core to understand engine-specific APIs (DuckDB PRAGMAs vs. Polars thread config vs. Daft runtime settings; DuckDB relations vs. Polars `LazyFrame`s vs. Daft `DataFrame`s), which is exactly the kind of cross-engine complexity multiplication tenet #3 exists to prevent — just relocated from "parallelism primitives" into "tuning and I/O primitives." The fix: keep this valuable, well-researched design work, but move all of it out of the core and into small, independent, opt-in pieces that a user reaches for only if they want them. None of what follows is imported or invoked by `duckpipe run` unless the user's own task code calls it.

**6.1 — Tuning helpers, as plain functions, not a core object.** No `ResourceBudget` class, no auto-detection wired into the scheduler. Instead, a small `duckpipe.tuning` module (or, better, a fully separate optional package so the core dependency tree doesn't even import `psutil`) offers pure functions like `suggest_duckdb_settings(mem_fraction=0.8) -> dict` or `suggest_thread_count(hyperthreading_discount=True) -> int`, built from the verified DuckDB internals below (§7). A user who wants tuned behavior calls one of these themselves, inside their own task, and applies it to their own connection (`con.execute(f"SET memory_limit='{settings['memory_limit']}'")`). This is a documented pattern, not a framework feature — it's what you proposed: light, pure-Python opt-in utilities, or just well-documented recipes, instead of baked-in behavior.

**6.2 — Cross-run/cross-process data handoff, as an opt-in caching backend.** The Tier 1/2/3 lazy-vs-eager-vs-Arrow design from the previous draft turns out to matter only when data actually needs to cross a real boundary — writing a `cache=True` task's output to disk so a later run can reuse it without re-executing upstream, or (Phase 3) shipping a result to another process/machine. In v1, `cache=True` can default to the simplest thing that works for literally any Python object — pickling — with zero engine awareness, satisfying tenet #2 completely. The Arrow-PyCapsule-based fast-path research (DuckDB/Polars/pandas/pyarrow all implement `__arrow_c_stream__`; Daft doesn't yet) becomes an **optional alternate cache backend** (`@task(cache=True, cache_backend="arrow")`) that a user opts into when their tasks move large tabular data and they want cheaper disk round-trips than pickle — genuinely useful, well-researched, but never the default and never something the core needs to understand to function correctly.

**6.3 — Docs, not framework code, carry the "lazy is the obvious choice" narrative.** DuckPipe's quickstart and scaffolded examples use `duckdb.sql(...)`/`pl.scan_parquet(...)`/lazy Daft frames because they're genuinely the best default for most pipelines, not because the framework requires or specially recognizes them. A short guide ("why your tasks should probably return lazy objects, and how to opt into faster caching if you do") carries this narrative instead of a lint rule or adapter layer baked into `@task`.

**6.4 — Where big-single-task distribution goes.** If a task genuinely needs cluster-scale, multi-machine data processing *within itself* (not DAG-level distribution), that's "use Daft's Flotilla/Ray runner inside that task" — the user's own code, the user's own dependency, nothing DuckPipe needs to wrap. Consistent with tenet #3. DuckPipe's own Phase 3 distribution story (§8) is about distributing *task orchestration* across ephemeral compute, not about distributing *a single task's* data processing.

This section is explicitly a lower-priority, later-phase area of investment (see §10) relative to the core scheduler/fingerprinting/CLI work — it should not gate v1.

---

## 7. Reference notes: DuckDB/Polars/Daft resource behavior (for the optional utilities in §6)

This section is *not* a core subsystem — it's the technical grounding for the opt-in `duckpipe.tuning` helpers in §6.1, kept here so the research underpinning them isn't lost, but explicitly marked as informing an optional layer rather than describing required orchestrator behavior.

**Threading.** DuckDB auto-detects host cores for its `threads` setting; over-launching on SMT/hyperthreaded machines can hurt performance (default to physical cores, not logical, except for I/O/remote-scan-heavy workloads, where DuckDB's own guidance suggests a 2–5× multiplier).

**Memory.** `memory_limit` defaults to ~80% of detected RAM and bounds DuckDB's buffer manager; spillable operators are hash aggregations, sorts, and joins. Per-thread rules of thumb from DuckDB's own docs: ~1–2GB/thread for aggregation-heavy workloads, ~3–4GB/thread for join-heavy ones — useful as the basis for a `suggest_duckdb_settings()` sanity check, entirely optional to apply.

**Disk.** `max_temp_directory_size` bounds spill-to-disk bytes (default ~90% of free space on the temp volume); some DuckDB versions have had temp-file cleanup edge cases worth a defensive cleanup helper if a user wants one (duckdb/duckdb#14142).

**Concurrency safety.** A single DuckDB connection isn't thread-safe for concurrent use; per-thread `cursor()` off a shared connection is the documented-safe pattern. DuckDB releases the GIL during query execution, so it already parallelizes internally via `threads` — a user stacking many Python threads each issuing heavy DuckDB queries risks oversubscribing the same core pool rather than adding throughput. This is genuinely useful for a user's own DAG-level fan-out code to know, but it's advice for task authors, not a scheduler feature.

**Streaming accessors.** DuckDB's current (non-deprecated) streaming Arrow accessor is `to_arrow_reader()`, returning a real `pyarrow.RecordBatchReader`. Polars' streaming engine (`collect(engine="streaming")`/`.sink_*()`) is recommended but still opt-in as of today, not default. Daft is lazy by default with two runners — Swordfish (single-machine streaming) and Flotilla (Ray-distributed) — see §6.4. None of this needs to be wired into DuckPipe; it's what the optional tuning/caching helpers in §6 are built from, and what the docs in §6.3 point users toward.

---

## 8. Serverless & distributed extension path (Phase 3+, mostly not v1)

Phase 2.5 already established the primitive this whole phase runs on: object-storage-native, put-if-absent conditional writes (`duckpipe.remote.locked()`), no server or catalog database involved. Phase 3 mostly asks what else that primitive makes possible, not what new infrastructure to add.

**The core move (§11 Phase 3a, implemented) — one new concept, and it's a narrowing of an existing one, not an addition beside it: `duckpipe.run(module, only=task_name)` (`--only` on the CLI) runs exactly one task instead of the whole DAG.** Locally this is trivial — build the DAG, execute one node instead of all of them, same file, same OS-level lock as always. Against a `state_uri`, it's what turns a single shared state file into something many nodes can contribute to at once, without ever generalizing `locked()` into something heavier:

- A scoped run downloads state as usual, absorbs any pending contributions from other concurrent invocations (below), decides skip-or-run for its one task exactly like today, and — if it ran — writes *only the rows it produced* to a small file under a unique key in the same bucket, instead of re-uploading the whole state file. A uniquely-keyed object can never collide with anyone else's write, so this needs no lock at all.
- "A small file" is not a new format: it's a `duckpipe.db` that happens to hold only a few rows, because that's the shape the existing `task_runs`/`task_fingerprints`/`task_cache` schema already has for "what one task run adds." Absorbing one is `ATTACH` plus a few `INSERT ... SELECT`, using tables that already exist. Every `state_uri` download absorbs any pending ones first, the same way it's always downloaded state first.
- What this buys: many nodes can each run `duckpipe run pipeline.py --only <task>` against the same `state_uri` at the same time, on different tasks — or even the same task redundantly, since fingerprint-based skip already makes redoing finished work harmless rather than corrupting. That's a real multi-node cluster run, on top of what Phase 1/2.5 already ship, no DuckLake or Quack required.
- Dispatch — deciding which node runs which task, in what order — stays outside DuckPipe, on purpose (tenet #1). `duckpipe show` already resolves topological order and previews skip-vs-run per task; that's the discovery primitive a coordinator (a Step Functions state machine, a Modal `.map()`, a shell loop over SSH) needs, and each dispatched unit is the same command any trigger already calls (§9), just narrower in scope. No scheduler, broker, or worker pool gets added to DuckPipe itself.

That covers "can this run on a cluster" — see it actually running one in `examples/04_distributed_cluster` (real worker processes, a `file://`-backed `state_uri`, swap for `s3://`/etc. and nothing else changes). Everything below is optional depth on top of it, not a prerequisite for it:

- **DuckLake-backed state (Phase 3b, implemented) — an observability upgrade, not a coordination one.** `db_path="ducklake:sqlite:pipeline.ducklake.sqlite"` — the same argument a plain file goes in, pointed at a different kind of string; `duckpipe run`/`show`/`stats` don't change at all. What it actually buys: every task's outcome becomes its own DuckLake snapshot, tagged with a plain-English commit message (`task extract succeeded`), so `task_runs AT (VERSION => n)` turns "what happened" into a real, queryable history instead of a present-tense table — and schema evolution (`ALTER TABLE ... ADD COLUMN`) needs no migration step. See `examples/06_ducklake_observability`.

  **Bonus, opt-in one layer further: the same catalog string can be backed by a dedicated, long-running metadata database instead of a local file** — `db_path="ducklake:postgres:dbname=... host=..."` (or MySQL), with `data_path` passed explicitly since there's no local file to derive a sibling directory from. Nothing about `duckpipe.run(...)` changes; `state.py`'s catalog-extension install and DATA_PATH handling are already generic across sqlite/duckdb/postgres/mysql, so this needed no new code, only checking it for real. What it buys over the SQLite catalog above: several DuckPipe deployments — different teams, tenants, or machines — sharing one catalog with genuine concurrent-write support, for teams that already run a central metadata database and want one. Verified directly, not assumed: 8 concurrent commits against a real Postgres catalog (via Docker, no retry logic) all succeeded, where the identical test against a SQLite catalog failed 3 of 8 outright (see `examples/05_distributed_with_ducklake`'s own SQLite-vs-Postgres writeup). Entirely optional, never required — the SQLite catalog above needs no such infrastructure and stays the right default for a single team's own history.

  Deliberately *not* wired to `state_uri`/`only=` (Phase 3a's own distributed mechanism) regardless of which catalog backs it — both raise clearly if combined with a `ducklake:` `db_path` — three independent, verified reasons why, not a hunch: (1) a SQLite catalog can't live on an object store (DuckLake's own maintainers confirm this), so it would still need the exact sync dance this backend exists to not need; (2) a Postgres/MySQL catalog removes that limitation but is provisioned infrastructure, which is why it stays opt-in rather than becoming Phase 3a's new default; (3) even against Postgres, DuckLake's optimistic concurrency control is retry-based (documented real failures under load in duckdb/ducklake#233, since fixed) — strictly weaker than the delta-merge mechanism's conflict-free-by-construction guarantee for "many small workers each report one fact." `examples/05_distributed_with_ducklake` demonstrates that narrower coordination use directly, on its own terms, separate from (and predating) Phase 3b's actual wiring into `duckpipe.run()`'s own state store described above.
- **Reference serverless executor (Phase 3c, implemented).** A task becomes a small function on Lambda/Modal/a container/etc.; each invocation is `duckpipe.run(module, only=task, state_uri=...)`. Deliberately *not* a bet on one platform: that call is already a plain Python function with no platform-specific glue, so "pick a reference platform" would mostly produce code whose only job is proving something already structurally true. What actually shipped is the demonstration, not a new dependency: `examples/07_serverless_executor` dispatches one DAG's two tasks through two genuinely different invocation shapes into the *same* distributed run — a container (`docker run`, argv-invoked, the way ECS/Cloud Run/a Kubernetes Job calls one) and a `handler(event, context)` function (the calling convention Lambda/Modal/most FaaS platforms actually use, no process/argv involved) — making the "not locked to one platform" claim checkable instead of asserted. **Quack** doesn't fit this path at all — it requires a persistent DuckDB server, a direct contradiction of "stateless executor" — so it's not part of DuckPipe's own sync layer. It remains available for a *coordinator* to optionally run for one distributed run's lifetime (spun up, used, torn down, the same shape as a Ray/Dask driver process), never as DuckPipe's default. This is also where large cross-invocation results genuinely benefit from the §6.2 Arrow-aware cache backend.
- **Remote "beefy node" mode (Phase 3d, implemented).** One job too large for a laptop but not worth distributing: the same code runs unchanged on a bigger remote machine, optionally paired with the §6.1 tuning helpers. Needs no coordination model at all — no `state_uri` locking, no delta files, no dispatch coordinator — just `rsync` the pipeline over, `ssh` in, run the exact same command. See `docs/remote_execution.md`.

Nothing here changes DuckPipe's task-authoring API (§5) — a task function written for local mode is the same function deployed to a Lambda executor, just invoked with a narrower scope. Tenet #5, one step further than Phase 1 needed it to go.

### Phase 4 — WASM/browser execution (first real deliverable shipped)

Promoted out of Phase 3's "aspirational" bucket once a real spike (not just documentation-reading) came back positive, then turned into `examples/08_browser_wasm` — a real, checkable adoption story, not a passing test buried in the suite. Everything below was verified directly, in-session, with DuckPipe's actual source loaded into a live Pyodide runtime, first via a Node.js harness for the initial spike and then via an actual headless browser (Playwright + Chromium) for the committed example — not assumed from DuckDB's own WASM marketing:

- The full `duckdb` Python package (real SQL engine, not a stub) loads via `pyodide.loadPackage("duckdb")` straight from Pyodide's own package repository (confirmed present, `duckdb==1.5.1`, in Pyodide 314's lock file — no PyPI round-trip needed).
- DuckPipe's core — `task.py`, `dag.py`, `fingerprint.py`, `remote.py`, `state.py`, `scheduler.py` — imports and runs a real multi-task pipeline through `scheduler.run()` inside that same Pyodide runtime with **zero code changes**, `asyncio.run()` included, in an actual browser engine (Chromium via Playwright), not just Node.
- The one real gotcha: `asyncio.run()` inside Pyodide needs the host to invoke Python via the promise-based entry point (`runPythonAsync`, not a plain sync `runPython`) and needs WASM JSPI (stack-switching) support in the JS engine. In a real browser this needs no flag at all (Node's `--experimental-wasm-stack-switching` requirement from the spike doesn't apply); it just worked once the harness called in correctly.
- JSPI itself is no longer experimental: standardized (W3C Wasm CG Phase 4) as of this writing, shipping in Chrome 137+ stable with no flag, Firefox 139 behind a flag, Safari committed with an engineer assigned.
- **The committed deliverable makes the case, not just the fact:** `examples/08_browser_wasm` is a real page — pick "your own file" and the bytes go straight from the file picker into Pyodide's virtual filesystem via `arrayBuffer()`, with zero network requests for the file itself (verified: a synthetic CSV profiled correctly with no fetch/XHR involved); reload the page — a genuinely fresh Pyodide instance — and every task reports `skipped`, because state persisted across the reload via `IDBFS`-backed IndexedDB, not just within one page's JS lifetime. `tests/test_examples.py::test_browser_wasm_example` drives an actual headless browser through exactly that cold-run-then-reload-then-rerun sequence (skipped automatically if `playwright install chromium` hasn't been run locally).

Honest, scoped-not-glossed-over limitations, unchanged from the spike and still true of the committed example: no `state_uri` remote sync and no DuckLake backend in-browser — Pyodide's DuckDB build has no runtime-loaded extensions (`httpfs`/`ducklake` unavailable), it's single-threaded (a Pyodide constraint on DuckDB's own internal parallelism, not on DuckPipe's `asyncio`-based task concurrency), and whether `fsspec` works against Pyodide's virtual filesystem/CORS-constrained fetch model is genuinely unverified — that's the next spike, not a claimed result.

---

## 9. Interoperability: embedding DuckPipe inside other orchestrators

A deliberately small addition, worth stating explicitly rather than leaving implicit: **DuckPipe should always be embeddable as a single task/step inside Airflow, Prefect, Dagster, or anything else, without any special integration code on DuckPipe's side.** This isn't a new subsystem — it's a free consequence of tenets #1 (no persistent daemon) and #5 (local dev and production are the same code path): a DuckPipe pipeline is just `duckpipe.run(module)`, an importable Python call that starts, does work, and exits, indistinguishable from any other function a `PythonOperator`, a Prefect `@task`, or a Dagster `@op` might wrap. The only discipline required is to never add something that assumes DuckPipe owns the top-level process (a global signal handler, an entrypoint with no clean importable form) — that's a constraint to preserve, not a feature to build.

**Where this is a genuinely good idea, not just a curiosity:**

- **Adoption wedge for teams that can't or won't replace their existing orchestrator.** A nightly transform expressed today as fifteen brittle, XCom-shuffling Airflow tasks can collapse into a single Airflow task that internally runs one DuckPipe pipeline — Airflow keeps doing what it's organizationally good at (cross-team scheduling visibility, existing alerting, audit trail), while the data-heavy inner loop gets DuckPipe's in-process data movement and fine-grained fingerprint-based incrementality. This is a far easier pitch than "replace your orchestrator," and a realistic path to it earning more territory over time.
- **Local dev/test parity.** A data engineer iterates against the DuckPipe pipeline directly (seconds, no scheduler/DB test harness) and only wraps the finished, unmodified module in a one-line task when deploying — directly addressing the "DAGs are hard to test locally" pain we researched against Airflow.
- **Fine-grained incrementality inside a fan-out unit.** If dynamic task mapping runs one sub-pipeline per partition, and that sub-pipeline is itself a small dependent-step DAG, each mapped instance can be "run a DuckPipe pipeline for partition X," giving step-level skip-if-unchanged behavior the outer platform's task-level caching can't express.

**Where to deliberately *not* go, because it's the exact scope creep the rest of this document exists to avoid:** no DuckPipe-side awareness of the host orchestrator. No Airflow provider package with custom operators/sensors/hooks, no DuckPipe retries deferring to the host's retry semantics, no fingerprints flowing through XCom. From the host's point of view, `duckpipe.run(module)` stays an opaque, atomic unit — success, failure, duration, nothing more. The entire "integration" should be a five-line documented recipe per platform, not a maintained package.

**Also worth stating plainly:** nesting DuckPipe inside a heavier platform is a decision driven by real governance/observability needs the outer platform provides (existing alerting, RBAC, multi-team visibility) — not something to do reflexively just because Airflow happens to already be installed. A pipeline that doesn't need any of that is still better served by plain cron/CI directly invoking DuckPipe (tenet #1), which stays lighter for that case.

**Documentation deliverable (Phase 2, tracked in §10):** a guide with working examples embedding a DuckPipe pipeline inside Airflow (`PythonOperator`), Dagster (`@op`), and Prefect (`@task`) — covering the one-task wrapper itself plus how to point DuckPipe's state file at durable storage (§2, tenet #1) so incrementality survives the host's ephemeral, container-per-task workers.

**Resolved (was an open concern):** if the same remote-synced state file could be touched by more than one concurrent invocation (two mapped partitions racing, or a retry overlapping a still-running attempt), a naive download→mutate→upload cycle risked a lost-update race. Phase 2 closed this with an external lease/lock object alongside the state file in the same bucket, using each backend's native conditional-write primitive rather than a new service — see §12, open question #5 for the full writeup of why this beat both a persistent Quack server and a full DuckLake catalog for this specific problem.

---

## 10. Competitive positioning (pain point → design response)

| Researched pain point | Source | DuckPipe response |
|---|---|---|
| Airflow: scheduler re-parses every DAG file on a fixed loop; degrades at scale | GitHub discussion #44727, Medium 60s→ms writeup | No persistent scheduler/parser process at all; "trigger" is just invoking the run command |
| Airflow (FLYR postmortem): CeleryExecutor requires pre-allocated workers; wastes spend; KubernetesExecutor caused unexplained task failures at high parallelism | FLYR Labs blog | No broker/worker-pool concept; scaling is "bigger single node" or Phase-3 stateless serverless, not a worker fleet to keep healthy |
| Airflow: XCom size limits, awkward inter-task data passing | Airflow docs/discussions | In-process task-to-task handoff is a plain function call (§2, tenet #2) — no serialization, no size cap, no side-channel at all in v1 |
| Prefect: Flow→Deployment→Flow Run hierarchy confuses users; not resolved in onboarding (issue closed "not planned") | GitHub #10811 | No separate "deployment" entity in v1 — a pipeline module *is* the runnable unit |
| Prefect: work pools/workers/blocks/task-runner menu — "which one do I need" is a recurring question | Prefect docs, linen.prefect.io threads | No work pools/workers/blocks in v1; one concurrency model in the core, with any engine-specific tuning left to the user's own code or the optional §6 utilities |
| Prefect: docs/tutorials steer users to wrap pandas in `DaskTaskRunner` instead of using the data engine's own parallelism | GitHub Discussion #3022, community tutorials | Explicit tenet (#3): the core never ships a competing parallelism or tuning primitive; an engine's own controls (or the opt-in helpers in §6) are the answer |
| Prefect: 1→2→3 breaking rewrites; migration pain even in typed codebases | Prefect migration guide, issue #15275 | Small, boring core API (§5) deliberately resists growth; task-authoring surface is the one thing we commit to keeping stable |
| Dagster: assets-vs-ops split-brain — two overlapping graph paradigms | Discussion #10512 | One task/dependency model, no parallel paradigm |
| Dagster: "I/O managers aren't required" but a default one is silently initialized anyway — contradicts stated minimalism | Issue #32065 | The core has no IO-manager concept to be silently mandatory about — data never passes through the framework at all in v1 (§2, §6) |
| No tool auto-tunes DuckDB resource settings to host specs | Our own DuckDB internals research — confirmed gap | Filled by the opt-in §6.1 utilities, deliberately kept outside the core so it never becomes a second engine-abstraction layer |
| No tool re-runs only what changed, by default, at the task level (dbt/SQLMesh do this for SQL models only) | SQLMesh docs, dbt `--state` docs | Fingerprint-based incrementality is default behavior for *any* task, not just SQL models |

---

## 11. Phased roadmap

**Phase 0 — Spike / validate core mechanics (1-2 weeks) — ✅ done**
- [x] Prove the fingerprinting + skip-if-unchanged model works end-to-end for a toy 4-task DAG. *(`tests/fixtures/toy_dag.py` + `tests/test_scheduler.py`; a diamond extract→{transform_a,transform_b}→load DAG with `cache=True` everywhere skips entirely on an unchanged re-run and fully re-executes under `--force`.)*
- [x] Prove dependency inference from function signatures is unambiguous and doesn't get weird with `*args`/type hints edge cases. *(Resolved via the mechanism itself, not a special case: an edge is a parameter whose **default value** is another `Task` object — see `src/duckpipe/task.py`. Type hints are never consulted, so two same-typed parameters are never ambiguous, and `*args`/`**kwargs` are structurally ineligible because Python syntax forbids defaults on them. Covered by `tests/test_task.py`.)*
- [x] Confirm cursor-sharing concurrency pattern behaves as expected under DAG-level fan-out with DuckDB's GIL-release behavior. *(`scripts/phase0_bench_fanout.py`: 4 concurrent DuckDB aggregations sharing one connection via `cursor()` + 4 sleep-based I/O-bound tasks, run through DuckPipe's scheduler vs. serially. Measured **2.2x speedup** on this machine — concurrent-cursor fan-out clearly beats serial execution.)*
- [x] Decide: pure-Python DAG scheduler (asyncio-based) vs. a minimal existing graph-execution library under the hood. *(Decided: built the small scheduler ourselves — `src/duckpipe/dag.py` (topological sort + cycle detection) and `src/duckpipe/scheduler.py` (asyncio + thread-pool execution). No third-party graph/execution framework pulled in.)*

**Phase 1 — v1 local orchestrator (the core deliverable, deliberately data-blind) — ✅ done**
- [x] `@task` decorator, signature-based dependency inference, `depends_on` escape hatch. *(`src/duckpipe/task.py`)*
- [x] `duckpipe run <module>` CLI; `duckpipe.db` state file with `task_runs`, `task_fingerprints`, `task_lineage` tables — documented schema, queryable by anyone. *(`src/duckpipe/cli.py`, `src/duckpipe/state.py`; also ships a `task_cache` table and a `pipeline_runs` table for run-level status.)*
- [x] Optional `fsspec`-backed state persistence (§2, tenet #1): a `state_uri` config that downloads the state file before a run and uploads it after. *(`src/duckpipe/remote.py`, lazily imported only when `state_uri` is set; `duckpipe[remote]`/`[s3]`/`[gcs]`/`[azure]` extras. Round-trip and "fresh ephemeral worker downloads state and sees skip-if-unchanged" both covered in `tests/test_remote.py`.)*
- [x] Fingerprint-based caching (`cache=True`), pickling the output by default — no engine-aware code in the core at all. *(`src/duckpipe/fingerprint.py`, `StateStore.get_cached`/`set_cached` in `src/duckpipe/state.py`. A task whose output can't be pickled — e.g. a live `DuckDBPyRelation` or a raw DB handle — logs a warning and degrades to "no incrementality for this task" rather than failing the run; see `tests/test_scheduler.py::test_uncacheable_output_warns_but_does_not_fail_the_run`.)*
- [x] Retries, basic structured logging, `--force` flag, `duckpipe show`. *(`@task(retries=N, retry_delay=...)`; `duckpipe show` prints the resolved DAG plus each task's last recorded status.)*
- [x] Explicitly *not* in Phase 1: any tuning helper, any Arrow-aware cache backend, any lazy/eager adapter. *(Confirmed: `src/duckpipe/` has zero imports of `psutil`, Polars, pandas, or pyarrow — those only appear in the dev/example dependency group.)*
- [x] Test suite emphasis / 3-5 realistic example pipelines. *(`examples/01_daily_batch_etl`, `examples/02_fanout_partitions`, `examples/03_mid_pipeline_materialization` — all run against bundled, real, open NYC TLC taxi data (`examples/data/`), all covered end-to-end by `tests/test_examples.py`, and all also run unmodified against the full public dataset via `DUCKPIPE_EXAMPLE_DATA` — see `examples/README.md`. The first two later grew apples-to-apples DuckDB/Polars variants (`duck.py`/`pl.py`) as part of the post-Phase-2 lazy/streaming pass described in §11's addendum below.)*

**Phase 2 — polish, real-world hardening, and the optional companion layer — ✅ done**
- [x] CI/cron/Lambda-handler recipes. *(`docs/triggers.md`: cron, GitHub Actions, Lambda handler, plain webhook — all reducing to one `duckpipe.run(...)` call. `.github/workflows/ci.yml` is a real, running instance of the GitHub Actions recipe, not just a doc snippet.)*
- [x] Failure/partial-DAG resume (re-run only failed + downstream tasks, using the same fingerprint mechanism). *(Turned out to need no new API: a failed task never gets a fingerprint/cache entry, so re-running the same command naturally re-executes only the failed task and its downstream, while unaffected `cache=True` upstream tasks skip via the existing incrementality mechanism — see `tests/test_scheduler.py::test_partial_dag_resume_reuses_cached_upstream_and_reruns_the_rest`. What genuinely needed building: a task cascade-skipped because its upstream failed now gets its own `task_runs` row (status `upstream_failed`) instead of vanishing from the state file entirely — that observability gap would have silently undermined both this resume story and `duckpipe stats`.)*
- [x] Observability: pre-built SQL views/small CLI reports rather than a UI server. *(`v_latest_task_status`, `v_run_summary`, `v_task_stats` in `src/duckpipe/state.py`'s `VIEWS_SQL`, queryable directly by any DuckDB client against the `.duckdb` file; `duckpipe stats` now reads from `v_task_stats` itself rather than duplicating the SQL. Also found and fixed a second observability gap alongside the resume work: a skip-if-unchanged cache hit never got a `task_runs` row either, which would have made every skip-count aggregation silently read zero.)*
- [x] Packaging/DX pass: error messages, docs, a "why DuckPipe" page. *(`docs/why-duckpipe.md` cites §10 pain points against actual file paths. CLI commands now catch `CycleError`/`DuplicateTaskNameError`/import errors and print one red line instead of a framework traceback — see `_friendly_errors` in `src/duckpipe/cli.py`. `duckpipe show` also gained a "next run" column previewing skip/run per task before you spend the time re-running.)*
- [x] Ship §6 as a genuinely separate, optional package: `duckpipe-tuning` (§6.1) and the Arrow-aware `cache_backend="arrow"` option (§6.2). *(`packages/duckpipe-tuning/` is a real second distribution in a uv workspace — `suggest_thread_count()`/`suggest_duckdb_settings()`/`suggest_temp_dir_limit()`, pure functions of host specs only, zero psutil/DuckDB coupling in `duckpipe` itself (verified by `packages/duckpipe-tuning/tests/test_tuning.py::test_helpers_never_import_duckdb`). `cache_backend="arrow"` lives in `src/duckpipe/state.py` behind a lazy `pyarrow` import and the `duckpipe[arrow]` extra, using `pa.table(value)`'s Arrow-PyCapsule support for DuckDB relations/pandas/Polars uniformly. Daft-PyCapsule gap (Eventual-Inc/Daft#2504) not yet closed upstream as of this writing — still documented as a known gap, not silently papered over.)*
- [x] Interoperability guide (§9): working embedding examples for Airflow, Dagster, and Prefect, plus the fsspec-backed remote state-sync recipe. *(`docs/interop.md`. Per §9's own stated boundary, these are five-line documented recipes, not maintained provider packages — no Airflow/Dagster/Prefect dependency was added anywhere in this repo.)*

**Phase 2.5 — hardening pass before Phase 3 — ✅ done** *(not in the original phase list; done as prep work once Phase 3 planning surfaced that open question #5 deserved another look before building on top of it, and that the examples should model lazy/streaming as the default harder than they had been.)*
- [x] Closed open question #5 for real, without needing a DuckLake catalog. Researched two heavier alternatives first and ruled both out for this specific problem: **Quack** needs a persistent DuckDB server process, exactly the standing infrastructure tenet #1 refuses; **DuckLake** gives genuine multi-writer ACID, but only once its catalog is itself a reachable, always-on database service — a SQLite-file catalog doesn't help because that catalog file would need the identical download-mutate-upload round-trip this problem is about, one level down. What shipped instead: an advisory lock object next to the state file, using each object-store backend's own native conditional-write primitive (S3 `If-None-Match`, GCS `if_generation_match`, Azure ETag preconditions) through fsspec's standard exclusive-create (`"x"`) file mode — no server, no catalog database. See `duckpipe.remote.locked()`, `StateLockedError`, and `duckpipe.run(..., lock=True)` (default) / `--no-lock`. This same put-if-absent primitive turned out to be the right foundation for Phase 3a's task-scoped concurrency too (§8) — Phase 3 mostly reuses it at a finer grain rather than needing something new.
- [x] Restructured the examples so laziness/streaming is the default posture, with a stated exception rather than an implicit one. `01_daily_batch_etl` and `02_fanout_partitions` are now apples-to-apples pairs (`duck.py` + `pl.py` in each folder) — same shape, two engines, both lazy/streaming end to end (Polars via `.sink_parquet()`/`scan_parquet()`, not `.collect()` then write). `03_incremental_sql_chain` (previously fully-eager-and-cached-at-every-step) became `03_mid_pipeline_materialization`: one deliberate eager checkpoint, with the docstring explaining exactly why that task and not the others (it's the only one that touches the full source file) — and a DuckDB→Polars engine handoff with zero glue code, illustrating tenet #2's data-blind core concretely.

**Phase 3 — distributed/serverless extension (§8) — ✅ done (3a, 3b, 3c, 3d)**
- [x] 3a: `only=`/`--only` task-scoped execution + delta-merge state. *(`duckpipe.run(module, only=task)` / `duckpipe run pipeline.py --only <task>` in `src/duckpipe/scheduler.py`; delta write/absorb primitives in `src/duckpipe/remote.py`; `StateStore.absorb_delta` in `src/duckpipe/state.py`; `duckpipe compact` CLI command for periodically folding `.pending/` back into the canonical file. `duckpipe show --json` is the discovery primitive an external coordinator uses. Two genuinely real bugs were caught by the test suite, not code review, while building this: `locked()`/`sync_up()` failed outright against a `state_uri` whose parent "directory" didn't exist yet on a local filesystem (fixed by `makedirs` before the write, harmless no-op on object stores); and a first draft had scoped runs delete the pending deltas they absorbed, which meant a worker starting later than another could never see what that other worker already contributed — fixed by only ever deleting deltas from a whole-run/`compact()` absorb, never a scoped one. Covered end to end, including genuine multi-process dispatch, by `tests/test_distributed.py` and the `examples/04_distributed_cluster` integration test.)*
- [x] 3b: DuckLake-backed state as an opt-in observability upgrade (§8). *(`db_path="ducklake:..."` in `src/duckpipe/state.py` — same argument a plain file goes in. Getting there required dropping `PRIMARY KEY`/`ON CONFLICT` from the whole schema, verified directly that DuckLake supports neither at all, in favor of one portable `DELETE`-then-`INSERT`/`INSERT ... WHERE NOT EXISTS` idiom used by *both* backends now — a simplification, not a workaround. `StateStore.transaction()` groups each task's writes into one commit and, on a DuckLake-backed store, tags it with a plain-English `commit_message` (`ducklake_snapshots()`'s `commit_message` column) — verified this was necessary, not cosmetic: `CREATE OR REPLACE VIEW` alone was found to commit a new snapshot on every single store open even when byte-identical, which would have flooded the exact run-history story this exists to make useful; fixed with `CREATE VIEW IF NOT EXISTS` instead. Added real `read_only=True` support (`StateStore`, `duckpipe show`/`stats`) so inspecting state never blocks on, or is blocked by, a run in progress — a gap that predates DuckLake and applied to the plain backend too. Deliberately rejects combination with `state_uri`/`only=`, with a clear error, not a silent no-op — see §8 for the three independently-verified reasons why. `examples/06_ducklake_observability` demonstrates time travel (`... AT (VERSION => n)`) and schema evolution (`ALTER TABLE ... ADD COLUMN`) concretely; `tests/test_ducklake.py` and CLI tests in `tests/test_cli.py` cover it end to end.)*
- [x] 3c: reference serverless executor. *(Turned out to need zero new library code — 3a's `duckpipe.run(module, only=task, state_uri=...)` already was the whole mechanism. What shipped instead is the checkable demonstration: `examples/07_serverless_executor` dispatches one DAG's two tasks into the *same* distributed run through two genuinely different invocation shapes — a `Dockerfile` (argv-invoked, `docker run`) and `handler.py` (`handler(event, context)`, the FaaS calling convention) — verified end to end by actually building the image and running both dispatches against a shared `file://` `state_uri`, not just documented (`tests/test_examples.py::test_serverless_executor_example`, skipped when no docker daemon is available). Quack confirmed out of this path entirely, per §8.)*
- [x] 3d: "bigger single node" remote execution mode. *(Confirmed to need no new coordination model at all — `docs/remote_execution.md` is the entire deliverable: `rsync` the pipeline over, `ssh` in, run the exact same `duckpipe run` command, optionally sized with the already-shipped §6.1 `duckpipe-tuning` helpers. No dedicated example/test: there is no new mechanism to exercise beyond what `tests/test_scheduler.py`/`tests/test_remote.py` already cover, the same reason `docs/triggers.md`'s Lambda recipe isn't itself deployed and tested.)*

**Phase 4 — WASM/browser execution (§8) — first real deliverable shipped**
- [x] Spike: does DuckPipe's core actually run inside a WASM sandbox, at all. *(Verified directly in a live Pyodide (Node) runtime, not assumed from documentation: `duckdb`-python installs via `micropip` and runs real SQL; DuckPipe's own `task.py`/`dag.py`/`fingerprint.py`/`remote.py`/`state.py`/`scheduler.py`, loaded as-is, ran a real two-task pipeline through `scheduler.run()` including `asyncio.run()`, with zero code changes — provided the host calls in via `runPythonAsync` and the JS engine has WASM JSPI (stack-switching), which is stable in Chrome 137+ today. See §8 for the full writeup and the honest list of what's *not* yet verified (`fsspec` in Pyodide, extension availability, threading implications).)*
- [x] First real deliverable: local-only browser tasks (no remote sync, no DuckLake backend). *(`examples/08_browser_wasm` — a real static page, DuckPipe's own source copied in verbatim by `prepare_bundle.py` (nothing rewritten), `duckdb` loaded straight from Pyodide's own package repo, state persisted across reloads via `IDBFS`/IndexedDB. Verified end to end with an actual headless browser (Playwright + Chromium, not just Node): a cold run against the bundled sample succeeds on all three tasks, a page reload plus re-run reports every task `skipped` (state genuinely survived the reload, not just the JS lifetime), and a user-picked local CSV gets profiled correctly with zero network requests for the file itself — the "your data never leaves the tab" claim, checked. `tests/test_examples.py::test_browser_wasm_example` automates the cold-run/reload/skip sequence, skipped cleanly if `playwright install chromium` hasn't been run locally.)*
- [ ] Next spike: `fsspec` against Pyodide's virtual filesystem/CORS-constrained fetch model — needed before `state_uri` sync (or the Phase 3b DuckLake backend) means anything in-browser. Not started; the committed deliverable above is deliberately local-only.

**Ongoing, from Phase 1 onward:** the "small, boring surface area" tenet needs a standing gate — any proposed new concept (a second decorator, a "profile" object, a plugin system) should be evaluated against "does this let us delete something instead," per the exact failure mode we saw Prefect fall into over three major versions.

---

## 12. Open questions to resolve before/while handing this to implementation

1. ~~**Dependency inference mechanics**~~ — **resolved.** An edge is a parameter whose *default value* is another `Task` object (`def b(x=a)`). Unambiguous (no type-hint parsing needed), structurally excludes `*args`/`**kwargs` (Python forbids defaults there), and needs no `Depends(...)` marker. See `src/duckpipe/task.py`'s module docstring, consistent with §5 above.
2. ~~**Fingerprint scope**~~ — **resolved for v1.** The hash covers the task function's own source (`inspect.getsource`), its declarative config, and upstream fingerprints — never closed-over globals or imported module versions, and never the output data. This means an upstream *external* state change (a file that changed on disk without the task's own code changing) is invisible to it by design; the accepted v1 answer is `@task(extra_fingerprint=[...])` — an opt-in list of anything hashable (a file mtime, a config value, an API version string) that also invalidates the cache when it changes. See `src/duckpipe/fingerprint.py`.
3. ~~**Pickle-as-default-cache limitations**~~ — **resolved.** Phase 1 shipped the size warning this question proposed (`LARGE_CACHE_WARN_BYTES` = 50MB in `src/duckpipe/state.py`), plus a related case this question didn't anticipate: some objects (a live `DuckDBPyRelation`, a raw DB handle) can't be pickled *at all* — that degrades to a logged warning rather than failing the run. Phase 2 shipped the `cache_backend="arrow"` alternative itself (§6.2): `pa.table(value)` handles DuckDB relations/pandas/Polars uniformly via the Arrow PyCapsule interface, behind a lazy `pyarrow` import and the `duckpipe[arrow]` extra. Daft's own PyCapsule support (Eventual-Inc/Daft#2504) is still pending upstream as of this writing — documented as a known gap, not silently papered over.
4. ~~**Boundary for the §6.1 tuning helpers**~~ — **resolved.** The concrete rule: every function in `duckpipe-tuning` takes only host specs (CPU count, total RAM, free disk) and returns a suggestion — none of them accept a DuckDB connection, run a query, or inspect data. Enforced by a real test (`packages/duckpipe-tuning/tests/test_tuning.py::test_helpers_never_import_duckdb`), not just a docstring promise. Shipped as a genuinely separate package (`packages/duckpipe-tuning/`), not a `duckpipe.tuning` submodule — psutil never enters `duckpipe`'s own dependency tree.
5. ~~**Concurrent access to a remote-synced state file (§2, §9)**~~ — **resolved, upgraded from the original plan.** This was originally accepted as documented last-writer-wins, but Phase 2.5 research (see `src/duckpipe/remote.py`) found a genuinely lighter fix than either heavier alternative it was weighed against: **Quack** (DuckDB's client-server protocol, real, targeting stable in DuckDB v2.0 Sept 2026) needs a persistent DuckDB *server* process — it solves multi-writer coordination only by reintroducing exactly the standing infrastructure tenet #1 refuses, so at most it's a fit for a coordinator process in Phase 3c's serverless executor (§8), not for this. **DuckLake** gives genuine multi-writer ACID, but only once its catalog is itself a reachable, always-on database service (Postgres, etc.); a SQLite-file catalog doesn't help here because that catalog file would need the exact same download-mutate-upload round-trip this problem is about, one level down — real DuckLake-backed state is correctly Phase 3b's job, an optional upgrade, not a Phase 2 shortcut. The fix that actually shipped: an advisory lock *object* next to the state file, written via each backend's native conditional-write primitive (S3 `If-None-Match`, GCS `if_generation_match`, Azure ETag preconditions) through fsspec's standard exclusive-create (`"x"`) file mode — no server, no catalog database, held for the whole download-run-upload sequence. See `duckpipe.remote.locked()`, wired into `duckpipe.run(..., state_uri=...)` by default (`lock=False`/`--no-lock` to opt out). A stale lock (crashed holder) is reclaimed after `max_lock_age` (default 1h) rather than deadlocking forever. This lock is deliberately scoped to *whole-run* invocations only — Phase 3a's `only=`/delta-merge mechanism (§8) is what extends safe concurrent writes to *task-scoped* invocations, without generalizing this lock into something heavier.
6. **Versioning/compatibility promise** — still open; a policy decision, not an implementation one. Given Prefect's breaking-rewrite pain was a named complaint (§10), worth deciding before any 1.0.
7. **Final project name** — still open; "duckpipe" is in active use as the working package/CLI name throughout this implementation (Phases 0-2, including the published-shape `duckpipe-tuning` companion package), but that's a placeholder, not a resolution — see §0. Resolve before publishing anything public.

---

## 13. Appendix — key verified facts underpinning this design

*(For implementer reference; sources are live links, verified August 2026. The DuckDB/Polars/Daft/Arrow-specific facts below ground the optional §6 utilities, not the core; the fsspec/DuckDB-remote-attach facts ground §2/§9's state-persistence design.)*

- DuckDB auto-detects host cores for default `threads`; morsel-driven parallelism needs enough row-group-sized morsels to use all threads; SMT/hyperthreaded over-launch can hurt performance. [DuckDB: Tuning Workloads](https://duckdb.org/docs/current/guides/performance/how_to_tune_workloads)
- `memory_limit` defaults to ~80% of detected RAM; spillable ops are hash aggregations, sorts, and joins; too-low a limit on a blocking operator can still throw `OutOfMemoryException`. [DuckDB: Memory Management](https://duckdb.org/2024/07/09/memory-management), [OOM Errors guide](https://duckdb.org/docs/lts/guides/troubleshooting/oom_errors)
- `max_temp_directory_size` bounds spill-to-disk bytes; some versions have had temp-file cleanup edge cases. [DuckDB Limits](https://duckdb.org/docs/current/operations_manual/limits), [duckdb/duckdb#14142](https://github.com/duckdb/duckdb/issues/14142)
- `DuckDBPyRelation` (from `.sql()`, `.table()`, `.read_parquet()`, etc.) is lazy and composes freely before execution. Its streaming Arrow accessor is now `to_arrow_reader()` — the older `fetch_arrow_reader()`/`fetch_record_batch()` names are deprecated as of 2026; `to_arrow_reader()` returns a real `pyarrow.RecordBatchReader` that streams batch-by-batch rather than materializing. DuckDB recommends partitioned Parquet read/write for larger-than-RAM workloads rather than full materialization. [DuckDB Quacks Arrow](https://duckdb.org/2021/12/03/duck-arrow), [DuckDB Arrow export docs](https://duckdb.org/docs/current/guides/python/export_arrow)
- **Polars `LazyFrame`** (`.scan_parquet()`, `.scan_csv()`, etc.) is lazy the same way. Its rewritten streaming engine (3–7x faster than the in-memory engine on larger data) is the *recommended* engine as of late 2025 but is still **opt-in**, not default — invoke via `lf.collect(engine="streaming")` or `.sink_parquet()`/other `.sink_*()` methods for a fully streaming write.  [Polars in Aggregate (Dec 2025)](https://pola.rs/posts/polars-in-aggregate-dec25/), [Polars streaming guide](https://docs.pola.rs/user-guide/concepts/streaming/)
- **Daft `DataFrame`** is lazy by default (builds a logical plan; executes on `.collect()`/`.show()`/`.write_parquet()`). Two runners: **Swordfish** (single-machine, Rust, streaming/out-of-core via async channels) and **Flotilla** (distributed, each Ray worker running Swordfish over its partition) — the latter is the natural answer when a single task genuinely needs multi-machine data processing (§6.4), rather than DuckPipe building its own distributed dataframe engine. [Daft Architecture](https://docs.daft.ai/en/stable/architecture/)
- **Arrow PyCapsule interface** (`__arrow_c_stream__`/`__arrow_c_array__`) is the standardized zero-copy interchange mechanism for eager objects; confirmed implemented by pyarrow, pandas (2.x+), Polars, and DuckDB — the basis for the optional §6.2 `cache_backend="arrow"`. Daft's eager-output support for this interface is still pending (tracked in Eventual-Inc/Daft#2504 as of this research), so a Daft-produced eager result would need `.to_arrow()` + `.to_batches()` chunking instead. [Narwhals + Arrow PyCapsule](https://labs.quansight.org/blog/narwhals-pycapsule), [Daft PyCapsule issue #2504](https://github.com/Eventual-Inc/Daft/issues/2504)
- A single DuckDB connection isn't thread-safe for concurrent use; per-thread `cursor()` off a shared connection is the documented-safe pattern; DuckDB releases the GIL during execution. [DuckDB: Multiple Python Threads](https://duckdb.org/docs/current/guides/python/multiple_threads), [duckdb-python discussion #40](https://github.com/duckdb/duckdb-python/discussions/40)
- **Quack protocol** (May 2026): DuckDB-to-DuckDB client-server communication over HTTP/TCP, multi-writer without lock contention, token auth, port 9494. [DuckDB blog](https://duckdb.org/2026/05/12/quack-remote-protocol)
- **DuckLake** (v1.0, April 2026): catalog (ACID SQL DB) + storage (Parquet) separation; concurrent multi-writer with ACID, time travel, schema evolution; production users include PostHog. [DuckLake](https://ducklake.select/), [DuckDB docs](https://duckdb.org/docs/current/core_extensions/ducklake)
- **DuckDB-WASM**: full engine compiled to WASM, runs in-browser or minimal edge builds (e.g., Cloudflare Workers); single-threaded by default, ~4GB memory ceiling. [DuckDB WASM docs](https://duckdb.org/docs/current/clients/wasm/overview)
- **DuckDB-python under Pyodide** (§8, Phase 4): the *Python* `duckdb` package, not just the JS `duckdb-wasm` library, is compiled to WebAssembly in its entirety and ships in Pyodide's own package repository — confirmed by DuckDB's own writeup and independently verified in-session by loading DuckPipe's real source and running a full pipeline through it. Its Pyodide build has no runtime-loaded extensions and can't reach remote files directly (pull them into Pyodide's virtual filesystem first). [DuckDB: DuckDB in Python in the Browser with Pyodide](https://duckdb.org/2024/10/02/pyodide)
- **WebAssembly JSPI (JavaScript Promise Integration)**: lets synchronous WASM code (e.g. Python's `asyncio.run()` inside Pyodide) block on an async JS operation without deadlocking the host event loop — required for DuckPipe's `scheduler.run()` to work unmodified inside Pyodide. Standardized (W3C Wasm CG Phase 4) as of this writing; shipping in Chrome 137+ stable with no flag, behind a flag in Firefox 139, committed (engineer assigned) in Safari. [V8: Introducing the WebAssembly JSPI API](https://v8.dev/blog/jspi), [Chrome for Developers: WebAssembly JSPI origin trial](https://developer.chrome.com/blog/webassembly-jspi-origin-trial)
- **DuckLake constraints verified directly, not assumed** (§8, Phase 3b): DuckLake supports no `PRIMARY KEY`/`UNIQUE` constraints at all (confirmed via `NotImplementedException` from the extension itself), so `ON CONFLICT` upserts are unusable against it. A SQLite (or DuckDB-file) catalog cannot live on object storage — a DuckLake maintainer states this is "a fondamental design decision, sqlite can not work on an object store" — so only a live network catalog (Postgres/MySQL, self-hosted or managed) gives a DuckLake-backed store genuine attach-directly-no-sync-dance behavior. [duckdb/ducklake discussion #519](https://github.com/duckdb/ducklake/discussions/519), [duckdb/ducklake issue #233 (concurrent writes)](https://github.com/duckdb/ducklake/issues/233)
- **Postgres-backed DuckLake catalog, verified directly against a real Postgres 17 instance** (§8, Phase 3b's bonus): `duckpipe.state`'s catalog-extension install and DATA_PATH handling needed zero code changes to work against `db_path="ducklake:postgres:..."` — confirmed with `StateStore` itself, including its `read_only=True` path and the CLI's `stats --snapshots`. The concurrency payoff is real, not just DuckLake's own marketing: 8 concurrent, no-retry commits against a fresh Postgres catalog all succeeded, where the identical test against a fresh SQLite catalog failed 3 of 8 outright with `TransactionException: ... database is locked` (matching `examples/05_distributed_with_ducklake`'s own earlier, separately-verified finding).
- SQLMesh's virtual-environment/fingerprint model (content-hash per model, environments as pointers over physical tables) is the direct inspiration for DuckPipe's task-level incrementality. [SQLMesh Overview](https://sqlmesh.readthedocs.io/en/stable/concepts/overview/)
- Landscape check found no existing project using a DuckDB file/catalog as the orchestrator's own coordination/state store, locally or across serverless invocations — this is the core validated gap. (yato, duckle, pydiverse.pipedag reviewed as closest prior art.)
- **`fsspec`** is the standard uniform Python filesystem abstraction (`AbstractFileSystem`, `fsspec.open()`), with `s3fs`/`gcsfs`/`adlfs` providing S3/GCS/Azure Blob support through the same interface local disk uses; it originated out of Dask specifically to avoid per-backend adapter code, and pandas/Dask/xarray all use it as their storage I/O layer. This is the basis for §2/§9's state-persistence design. [fsspec docs](https://filesystem-spec.readthedocs.io/en/latest/), [fsspec background](https://filesystem-spec.readthedocs.io/en/latest/intro.html)
- **DuckDB's own database file format supports remote `ATTACH` (via `httpfs`, e.g. `ATTACH 's3://bucket/file.duckdb'`) for read-only access only** — its docs state explicitly that writing the database via HTTPS or the S3 API is not possible; read-write access is filesystem-local only. This confirms a download-before-run/upload-after-run sync pattern (not a live remote-attached database) is the correct, currently-only approach for a read-write state file on object storage. [DuckDB: Attach over HTTPS or S3](https://duckdb.org/docs/current/guides/network_cloud_storage/duckdb_over_https_or_s3), [duckdb/duckdb#10967](https://github.com/duckdb/duckdb/issues/10967)
